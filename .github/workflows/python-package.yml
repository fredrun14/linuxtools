name: Python Package

on:
  push:
    branches: [master, main]
    tags:
      - 'v*'
  pull_request:
    branches: [master, main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pycodestyle

      - name: Check PEP8 compliance
        run: pycodestyle src/linuxtools/

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.13', '3.14']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install uv
          pip install -e ".[dev]"

      - name: Type check (mypy strict)
        run: mypy src/linuxtools/

      - name: Run tests
        run: pytest tests/ -v

  # L'upload d'artefact diffère selon la forge : le backend d'artefacts de
  # Forgejo refuse actions/upload-artifact@v4 (« not supported on GHES »), et
  # cette action est requise sur GitHub. Aucune version unique ne marche sur les
  # deux. Un STEP sauté par `if:` ne suffit pas — GitHub résout TOUTES les
  # actions d'un job au démarrage, même les steps sautés. On sépare donc en deux
  # JOBS gardés au niveau job : un job sauté n'est jamais initialisé, donc son
  # action (forgejo/upload-artifact) n'est jamais résolue sur l'autre forge.
  # Aiguillage ancré sur github.com : le runner Forgejo rapporte son URL INTERNE
  # (ex. http://192.168.50.102:3300), pas le nom externe — donc on ne peut pas
  # « détecter Forgejo » par son URL. On détecte GitHub (URL stable) et tout le
  # reste est traité comme Forgejo. Variable de contexte, aucun appel réseau.
  build-github:
    if: ${{ github.server_url == 'https://github.com' }}
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build package
        run: python -m build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  build-forgejo:
    if: ${{ github.server_url != 'https://github.com' }}
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build package
        run: python -m build

      - name: Upload artifacts
        uses: forgejo/upload-artifact@v4
        with:
          name: dist
          path: dist/
